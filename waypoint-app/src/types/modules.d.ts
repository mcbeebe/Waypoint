/**
 * Type declarations for third-party modules missing TypeScript definitions.
 * (React Navigation v7 ships its own types — shims removed in roadmap 0.1
 * so stack/tab navigators and route params are fully typed.)
 */
