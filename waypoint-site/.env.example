# waypoint-site environment variables
# Copy to .env.local for local dev; set the production values in the Vercel
# project's Environment Variables (Production scope).

# GA4 Measurement ID (e.g. G-XXXXXXXXXX). Second, additional tracker alongside
# Plausible — see docs/analytics-taxonomy.md (D3, "Providers"). Leave unset in
# preview/dev; the GA4 snippet in BaseLayout.astro no-ops without it.
GA_MEASUREMENT_ID=
